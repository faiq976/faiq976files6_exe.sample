#include<iostream>
using namespace std;
void convert(int digit,string words, string num);
int digit;
string words;
string num;
string num[100];
for(int i=0; i<string num; i++)
{
   int digit=num[i];
   cout<<words[digit]<<""<<endl;
}
int main()
{
    convert(string words)
    cout<<"Enter numbers:";
    cin>>num;
}