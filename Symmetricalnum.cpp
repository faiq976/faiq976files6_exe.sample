#include <iostream>
using namespace std;
int main()
{
    int num, symmetrical, unsymmtrical;
    cout << "Enter a 3-digit number:";
    cin >> num;
    for (int i = 0; i <= 2; i++)
    {
        for (int i = 0; i <= 2, i--)
        {
            if (condition = symmetrical)
            cout << "Number is symmetrical";
               cin >> symmetrical;
        }
        else
        {
            cout << "Number is unsymmetrical";
                 cin >> unsymmetrical;
        }
        return 0;
    }
}